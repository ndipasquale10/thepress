{
  "name": "greenside-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor App Store / Play Store wrapper for the Greenside (The Press) golf betting web app. Wraps the untouched web app at the repo root.",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "sync": "npm run build:web && cap sync",
    "add:ios": "npm run build:web && cap add ios && cap sync",
    "add:android": "npm run build:web && cap add android && cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android",
    "icons": "capacitor-assets generate --assetPath resources"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/haptics": "^6.0.1",
    "@capacitor/status-bar": "^6.0.1",
    "@capacitor/splash-screen": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@capacitor/assets": "^3.0.5"
  }
}
